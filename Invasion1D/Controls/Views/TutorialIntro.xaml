<?xml version="1.0" encoding="utf-8" ?>
<ContentView 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Invasion1D.Controls.Views"
    x:Class="Invasion1D.Controls.Views.TutorialIntro">
    <Grid
        RowDefinitions="*,auto">
        <ScrollView>
            <VerticalStackLayout>
                <Label 
                    Text="Welcome"
                    Style="{StaticResource Title}"/>
                <Label 
                    Text="This might seem overwhelming but its preety simple, first the weird words: Vitalux its an attack, Weave is special attack and Warpium its an item that allows teleportation."
                    Style="{StaticResource Text}"/>
                <Label 
                    Text="Invasion 1D is a 1st person shooter but in one dimention, so you can only move one way or the oposite way, the world only has one axis. The only way you can diferentiate objects is by color and they fade with distance."
                    Style="{StaticResource Text}"/>
                <Label 
                    Text="You have to manage the Vitalux and Warpium powers to survive and clear all enemies. Weave is a special power that temporarly allows free and fast attacks. "
                    Style="{StaticResource Text}"/>
                <Label 
                    Text="Remember: Whats on the center of the view is in front of you and what is on the periferal vision is behind."
                    Style="{StaticResource Text}"/>
                <Label 
                    Text="In this tutorial you can see the multiverse from a higher dimention."
                    Style="{StaticResource Text}"/>
                <Label 
                    Text="The following table is an Info sheet, you can refer to it anytime in the pause menu."
                    Style="{StaticResource Text}"/>
                <local:InfoSheet/>
                <Label 
                    Text="Ready?"
                    Style="{StaticResource Text}"/>
            </VerticalStackLayout>
        </ScrollView>
        <Button 
            Text="Start Tutorial"
            Clicked="StartTutorialButton_Clicked"
            Style="{StaticResource VerticalMenuButton}"
            Grid.Row="1"/>
    </Grid>
</ContentView>
